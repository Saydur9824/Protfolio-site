import React from 'react';
import resume from '../../images/resume.PNG'

const Resume = () => {
    return (
        <div>
            <img src={resume} alt=""/>
        </div>
    );
};

export default Resume;